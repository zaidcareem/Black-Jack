Please do not run the program in local terminal.
Run it in a terminal of an IDE.

For example run the program in VS Code's or PyCharm's terminal.

Program runs in both local terminal as well as IDE terminal.
But colors are not displayed in local terminal. Unlike the IDE/Text Editor's terminals.